# v1.0.0 — Лабораторная работа № 1

Первый учебный релиз содержит полностью воспроизводимую реализацию лабораторной
работы «Подготовка стенда» и модели экспоненциального роста.

## Основные результаты

- подготовлены каталоги всех восьми лабораторных работ;
- реализованы одиночный и параметрический расчёты уравнения `du/dt = αu`;
- численное решение проверено относительно аналитической формулы;
- сгенерированы и выполнены два Jupyter-ноутбука;
- сгенерированы два автономных Quarto HTML-документа;
- сохранены таблицы результатов и пять графиков;
- тестовый набор проходит полностью: `3/3`.

## Воспроизведение

```bash
cd labs/lab01/project
docker compose build
docker compose run --rm lab01 make setup
docker compose run --rm lab01 make all
```
